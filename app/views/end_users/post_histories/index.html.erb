<div class="main">
  <%= render "layouts/navbar" %>
  <section class="user-posts">
    <h4><%= link_to "戻る", 'javascript:history.back()' %></h4>
    <div class="user-posts_top">
      <div class="user-posts_user-title"><%= attachment_image_tag @end_user, :user_image, size: "100x100", fallback: "no_image.jpg" %>
        <h4><%= @end_user.name %></h4>
      </div>
      <div class="user-posts_post-search">
      </div>
    </div>

    <div class="user-posts_page">
      <%= paginate @posts %>
    </div>

    <div class="user-posts_index">
      <% @posts.each do |post| %>
        <div class="user-posts_post">
          <div class="user-posts_post-main">
            <div class="user-posts_post-user">
            <%= link_to user_page_path(post.end_user.id), class:"user-posts_user-link" do %>
              <%= attachment_image_tag post.end_user, :user_image, size: "70x70", fallback: "no_image.jpg" %>
              <%=  post.end_user.name %>
            <% end %>
              <div class="user-posts_post-created">
                <div class="user-posts_post-created-time"><%= post.created_at.strftime("%Y/%m/%d/ %H:%M") %></div>
                <div class="user-posts_post-created-hobby"><%= post.hobby.title %></div>
              </div>
            </div>

            <%= link_to hobby_post_path(post.hobby, post), class:"user-posts_post-link" do %>
              <h4 class="user-posts_post-title"><%= post.title %></h4>
              <div class="user-posts_post-image">
                <%= attachment_image_tag post, :post_image, size: "500x300" %>
              </div>
              <div class="user-posts_post-list">
                <% post.tags.each do |tag| %>
                  <span class="user-posts_post-tag">#<%= tag.name %></span>
                <% end %>
              </div>
            <% end %>
          </div>
          <div class="user-posts_post-sub">
            <h6 class="user-posts_post-comment"><%= image_tag "comment.jpg" %><%= post.post_comments.count %>件</h6>
            <div class="user-posts_post-favorite">
              <% if post.favorite_posts?(current_end_user) %>
                <h6>いいね♥<%= post.favorite_posts.count %></h6>
              <% elsif %>
                <h6>いいね♡<%= post.favorite_posts.count %></h6>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
</div>